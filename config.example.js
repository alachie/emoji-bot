const config = {
	token: 'token',
	minPermission: 'ADMINISTRATOR',
	serverChannels: [
		{
			serverId: '', // the ID of the server 
			channelId: '', // and the channel ID you want the bot to post in on that server
		},
	],
	maxPerLine: 26
}

module.exports = config
